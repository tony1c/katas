'use strict';

/////////////////////////////////
// Bubble sort algo!

const arr = [13, 9, 1, 4, 3, 101, 74, 99, 7, 77];
const arr1 = ['b', 'a', 'z', 'g', 'c'];

console.log(bubbleSort(arr));
console.log(bubbleSort(arr1));
