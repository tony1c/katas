'use strict';
/*
const s1 = '6900690040';
const s2 = '4690606946';
const s3 = '9990494604';

function compareStr(str1, str2) {
  const maxLength = Math.max(str1.length, str2.length);
  let output = Array(maxLength).fill(0);
  let result = 0;

  for (let i = 0; i < maxLength; i++) {
    if (str1[i] === str2[i]) output[i]++;
  }
  // output = output.filter((val) => val !== 0);
  // return output;
  for (let i = 0; i < output.length; i++) {
    if (output[i] !== 0) result += output[i];
  }
  return `${result} common positions out of ten`;
}

console.log(compareStr(s1, s2));
console.log(compareStr(s1, s3));
console.log(compareStr(s2, s3));

function comp(a, b) {
  const maxLength = Math.max(a.length, b.length);
  let output = [];

  for (let i = 0; i < maxLength; i++) {
    output.push(Math.sqrt(b[i]));
  }
  return output;
}

const a = [11, 144, 19, 161, 19, 144, 19, 11];
const b = [121, 14641, 20736, 361, 25921, 361, 20736, 361];
console.log(comp(a, b));


function is_array(any) {
  return Array.isArray(any);
}

console.log(is_array('w3resource'));
console.log(is_array([1, 2, 4, 0]));

*/
